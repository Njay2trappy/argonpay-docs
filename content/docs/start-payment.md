# Start payment (unified)

> Start monitoring a pending payment on any supported network with `StartPayment`.

GraphQL-only unified starter. REST `POST /pay` routes here when `apiKey` or Bearer auth is provided.
